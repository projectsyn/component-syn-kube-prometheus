---
parameters:
  prometheus:
    namespaces:
      syn-prometheus: {}
      syn-not-prometheus: {}
    defaultInstance: default-instance
    instances:
      default-instance:
        prometheus:
          enabled: true
      other_instance:
        prometheus:
          enabled: true
    addons:
      - cluster-monitoring
