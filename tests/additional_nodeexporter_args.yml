---
parameters:
  prometheus:
    instances:
      default-instance:
        nodeExporter:
          enabled: true
          containers:
            nodeExporter:
              additionalArgs:
                - --collector.cpu.info
