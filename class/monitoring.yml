parameters:
  kapitan:
    compile:
      - input_paths:
          - monitoring/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - monitoring/component/main.jsonnet
        input_type: jsonnet
        output_path: monitoring/
